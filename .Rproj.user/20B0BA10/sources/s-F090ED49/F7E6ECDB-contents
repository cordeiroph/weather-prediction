dataDescription <- function(id){
  ns <- NS(id)
  
  htmlTemplate("www/dataDescription.Rhtml")
  
}

dataDescriptionModule <- function(input, output, session, dataSet) {
  
  output$summary <- renderPrint({
    summary(dataSet)
  })

}
